# AI Assistant: Настольный ИИ-ассистент

!C# !AvaloniaUI !AI

Настольный ассистент на базе ИИ с графическим интерфейсом для выполнения системных команд, генерации кода, математических вычислений и многого другого.

## Демонстрация

*(Здесь будет размещена GIF-анимация или встроенное видео, демонстрирующее работу CLI-ассистента)*

## Описание проекта

Этот проект представляет собой десктопное приложение на Avalonia UI, которое объединяет возможности больших языковых моделей (LLM) с прямым доступом к системным функциям.

Основная сложность заключалась в создании надежного "моста" между естественным языком пользователя и командами, которые может выполнить операционная система или программный код. Проект решает эту проблему с помощью следующей архитектуры:
1.  **Парсер команд:** `MainWindowViewModel` содержит основную логику, которая анализирует ввод пользователя для определения его намерения (например, "создай файл", "открыть сайт", "спроси ИИ").
2.  **Интеграция с ИИ:** Для сложных или неструктурированных запросов используется `AIService`, который обращается к LLM (например, через OpenRouter API). Уникальная особенность — ИИ может сам предложить системную команду для выполнения, которую пользователь может подтвердить.
3.  **Выполнение команд:** Приложение напрямую использует API .NET для взаимодействия с файловой системой (`System.IO`), запуска процессов (`System.Diagnostics.Process`) и выполнения математических вычислений.

В результате получился гибкий инструмент с современным графическим интерфейсом, способный как выполнять простые системные задачи, так и решать комплексные проблемы с помощью искусственного интеллекта.

## 🚀 Ключевые возможности

*   **🧠 Взаимодействие с ИИ:** Отправляйте запросы напрямую в языковую модель (например, GPT) для получения ответов на сложные вопросы.
*   **🌐 Открытие сайтов:** Открывает веб-сайты по URL или даже по простому названию (например, `открыть сайт youtube.com`).
*   **⚙️ Взаимодействие с файловой системой:** Создавайте, читайте, удаляйте файлы и папки, а также просматривайте содержимое директорий с помощью команд на естественном языке.
*   **🧮 Математические вычисления:** Решайте математические выражения и уравнения.
*   ** Контекст диалога:** Ассистент запоминает предыдущие вопросы и ответы в рамках сессии для более осмысленного общения.
*   **✨ Современный UI:** Графический интерфейс, созданный с помощью Avalonia UI, с эффектом прозрачности "Acrylic" и удобным представлением истории команд.
*   **💾 Кэширование:** Ответы от ИИ кэшируются для ускорения повторных запросов.

## 🛠️ Технологический стек

*   **Язык:** C# (.NET 8)
*   **UI Фреймворк:** Avalonia UI
*   **Архитектурный паттерн:** MVVM (с использованием CommunityToolkit.Mvvm)
*   **Ключевые библиотеки:**
    *   `System.Net.Http.HttpClient` для взаимодействия с API.
    *   `System.Text.Json` для работы с JSON.

## ⚙️ Установка и запуск

*(Руководство по установке, настройке переменных окружения (например, `OPENROUTER_API_KEY`) и запуску проекта будет добавлено позже.)*

## 📝 Подробности реализации

Приложение построено на паттерне MVVM. Рабочий процесс выглядит следующим образом:

1.  **Ввод пользователя:** Пользователь вводит команду в `TextBox` в `MainWindow.axaml`. Ввод привязан к свойству `Command` в `MainWindowViewModel`.
2.  **Выполнение команды:** Нажатие кнопки "Выполнить" вызывает `ExecuteCommandCommand`.
3.  **Парсинг и маршрутизация:** Метод `ExecuteCommand` анализирует строку команды с помощью серии `if-else if` и определяет тип действия.
4.  **Обработка:**
    *   Для запросов к ИИ (`спроси`, `?`) вызывается `AIService`, который отправляет HTTP-запрос к API (например, OpenRouter), предварительно проверяя кэш.
    *   Для файловых операций (`создать файл`, `dir`, `читать`, `удалить`) используются методы из `System.IO`.
    *   Для открытия сайтов используется `System.Diagnostics.Process.Start`.
5.  **Вывод результата:** Результат операции или ответ от ИИ записывается в свойство `Output` и добавляется в коллекцию `History`, что приводит к обновлению UI.

```mermaid
graph TD
    A[Пользователь вводит команду в UI] --> B{MainWindowViewModel};
    B -->|Запрос к ИИ| C[AIService];
    B -->|Файловая операция| D[System.IO API];
    B -->|Открыть сайт| E[System.Diagnostics.Process];
    C --> F[CacheService];
    F --> G[OpenRouter API];
    G --> H{Результат};
    D --> H;
    E --> H;
    H --> B;
    B --> I[Обновление UI (History, Output)];
```

## 🗺️ Перспективы развития (Roadmap)

*   **Безопасность:** Улучшение обработки команд для предотвращения выполнения опасных системных вызовов. Внедрение "песочницы" или режима подтверждения для рискованных операций.
*   **Расширение функционала:**
    *   Добавление поддержки плагинов.
    *   Интеграция с другими API (погода, новости, биржевые котировки).
    *   Поддержка ведения контекста диалога.
*   **Оптимизация:** Рефакторинг текущей кодовой базы для повышения производительности и масштабируемости.
*   **Редизайн:** В будущем возможен переход на более продвинутый UI-фреймворк для терминала или полный редизайн архитектуры.